# System Health Monitor

A Python script that monitors CPU, RAM and disk usage on Linux.

## Features
- Live system stats
- Warnings when usage is high
- Logs data to CSV with timestamps
- Runs automatically every 60 seconds

## Requirements
- Python 3
- psutil

## Install
pip install psutil

## Usage
python practice.py
